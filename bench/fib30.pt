// Benchmark 1: Recursive Fibonacci (n=30 for PT)
fn fib(n) {
  if (n <= 1) return n;
  return fib(n - 1) + fib(n - 2);
}

let start = clock();
let result = fib(30);
let elapsed = clock() - start;
show("fib(30) = " + toString(result));
show("Time: " + toString(elapsed) + "s");
