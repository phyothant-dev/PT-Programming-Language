// Benchmark 1: Recursive Fibonacci
fn fib(n) {
  if (n <= 1) return n;
  return fib(n - 1) + fib(n - 2);
}

let start = clock();
let result = fib(35);
let elapsed = clock() - start;
show("fib(35) = " + toString(result));
show("Time: " + toString(elapsed) + "s");
